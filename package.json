{
  "name": "todo-list-app",
  "version": "1.0.0",
  "description": "A simple to-do list desktop application built with Electron",
  "main": "main.js",
  "scripts": {
      "start": "electron .",
      "package": "electron-packager . todo-list-app --platform=win32 --arch=ia32 --out=dist --overwrite"
  },
  "devDependencies": {
      "electron": "^25.3.1",
      "electron-packager": "^15.3.0",
      "electron-installer-windows": "^4.0.0"
  }
}
